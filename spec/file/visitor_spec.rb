require 'spec_helper'

describe File::Visitor do
  it 'has a version number' do
    expect(File::Visitor::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
